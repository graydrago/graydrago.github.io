export interface Meta {
  title: string;
  description: string;
  sourceCode: string;
  onlineExample: string;
  ordering: number;
}

export interface BBox {
  x: number;
  y: number;
  width: number;
  height: number;
}
