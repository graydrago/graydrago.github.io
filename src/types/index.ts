export interface Meta {
  title: string;
  description: string;
  sourceCode: string;
  onlineExample: string;
}

export interface BBox {
  x: number;
  y: number;
  width: number;
  height: number;
}
